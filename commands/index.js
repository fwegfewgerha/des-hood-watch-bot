const embed = require('./embed');
const stats = require('./stats');
const raidprotect = require('./raidprotect');
const secure = require('./secure');
const modlog = require('./modlog');

module.exports = [embed, stats, raidprotect, secure, modlog];
